function Anik(){
    return (
        <>
        <h1>Anik knows</h1>
        <b>REACT</b>
        </>
    )
}

export default Anik

/**NOTE:-
 * 
 * 1.Always try to keep the name of a function in capitalized format and
 *  it is best practice to also have the file name in the capitalize format 
 * though it doesn't show any error in case of file naming but it shows a error
 *  in case of function naming so try to give the function name in capitalize format
 *  and the file name as per your utility.
 * 
 */