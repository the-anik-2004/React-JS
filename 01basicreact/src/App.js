import Anik from "./Anik";
function App() {
  return (
    <>
     <h1>Welcome to react| ANIK PAL 🎉</h1>
     <Anik/>
    </>
 
  
  );
}

export default App;
/**NOTE:-
 * 
 * 1.In JS you cannot export more than one component so in case if you want to export more than one component you have to wrap it on in the one component.
 * Here you can use relative element to wrap all the components
 * Or you can use "<>...</>" [known as FRAGMENTS]
 * 
 */
